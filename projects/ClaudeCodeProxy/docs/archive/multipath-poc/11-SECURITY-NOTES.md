* **Keep keys in env** (`ZAI_API_KEY`, optional `ANTHROPIC_API_KEY` for HTTP gateway only).
* **Never** commit keys.
* Mask prompts/paths in logs when sharing.
* Minimal file permissions on `logs/` and `work/` folders.

---