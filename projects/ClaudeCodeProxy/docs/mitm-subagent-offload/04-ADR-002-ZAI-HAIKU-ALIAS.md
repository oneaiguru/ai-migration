**Decision**

* Haiku lane is replaced with **GLM** on Z.AI endpoint.
* Sonnet/Opus remain on Anthropic subscription.

**Notes**

* Z.AI’s Claude Code integration endorses `ANTHROPIC_BASE_URL` + key.
* In Subagent runs, set model alias or detect `model` containing `haiku`.

---