* **Claude Code CLI:** pin to `>=2.0.14`.
* **mitmproxy:** pin to a recent stable (e.g., 10.x); agent records exact tested version.
* Record with `scripts/print-versions.sh` and write into `VERSIONS.json`.

---