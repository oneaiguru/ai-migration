* **P0**: Subagent offload via MITM + logging → ship
* **P1**: Session‑level split (launch GLM session for throughput tasks)
* **P2**: Per‑turn model routing (after proxy/H2 validation)
* **P3**: GUI helper / Electron wrapper (optional)

---