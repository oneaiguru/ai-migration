```
# pseudo-make (use npm scripts or bash)

make mitm       # start mitmdump with addon
make sub        # start Anthropic subscription session (work/sub)
make zai        # start Z.AI session (work/zai)
make test       # run T1..T6 from 12-TEST-MATRIX.md
make logs       # tail -f logs/usage.jsonl
make versions   # print versions into VERSIONS.json
```

---