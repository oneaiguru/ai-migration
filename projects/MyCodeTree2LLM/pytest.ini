[pytest]
# Ensure pytest-bdd recognizes feature files
bdd_features_base_dir = features

# Ensure pytest includes the features directory in test discovery
testpaths = tests features

# Enable verbose and gherkin terminal reporting
addopts = --gherkin-terminal-reporter --verbose
