# Session Ideas – 2025-10-20

## Telemetry & Measurement
- Context reclaim metrics (track delta between successive `/status` panes to quantify pruning; surface in preview).
- Reset anomaly watchdog (record observed reset moments vs reported `reset_at`, flag mismatches).
- Schema/tool version stamping on JSONL records for provenance (prep for cross-session analytics).

## Automation & Capture
- Claude `/usage` automation wrapper mirroring Codex script (fixtures + behave + recipe).
- Automation QA harness: scheduled live run that verifies alias ingestion (Codex & Claude) and records anomalies.

## Docs & Handoff
- Deliverables bundle script (zip plan + TLDR + ledgers + timeline) for consistent handoffs.
- Tracker aliases wiki entry + SOP pointers to shrink onboarding time.

## Guardrails & QA
- Alias delete regression suite (multi-window undo, ensures JSONL rewrite + state recompute).
- ccusage live export tests (daily/weekly) with fixtures and preview assertions.

## Cleanup / Backlog Hygiene
- Promote quick ideas into backlog specs (context metrics, reset watchdog, automation QA) for next planning pass.
