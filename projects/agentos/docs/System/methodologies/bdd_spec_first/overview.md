# BDD Spec-first Workflow

**Goal:** Ensure every tracker behaviour is defined via feature files before implementation, aligning with the SOP BDD section.

**Scope:**
- `features/*.feature` and `features/steps/`
- Pytest companions that backstop parser logic
- Documentation updates enforcing `behave features` in validation loops

**Entry Docs:**
- `docs/SOP/standard-operating-procedures.md#bdd-workflow-tracker-tooling`
- `docs/Tasks/bdd_tracker_standardization.md`
- `docs/Tasks/tracker_cli_todo.md` (BDD coverage block)
