# Forecasting & Analytics – UAT Session Prompt (2025‑11‑07 12:00)

Single message for the browser agent. Paste everything below as-is after uploading any text notes you plan to attach (no images required).

---

**Task**: Re-run the Stage 6 forecasting parity checks on the latest demo build and document results as textual notes (no screenshots).

**Demo under test**: https://forecasting-analytics-3pemuiun9-granins-projects.vercel.app

**Reference** (optional, read-only): https://wfm-practice51.dc.oswfm.ru (log in via the OIDC URL + credentials below).

**Credentials**:
- Username: `user20082025`
- Password: `user20082025`
- OIDC URL: use the macro in the prompt deck (`Ctrl+L`, paste URL, then enter credentials).

**Flows to execute (parity_static + chart_visual_spec subset)**
1. Build Forecast (`/build`)
   - Select a queue and horizon, run **«Построить прогноз»**.
   - Trigger **Загрузить прогноз** / **Загрузить факт** templates.
   - Note whether any banners, toasts, or bell notifications appear and whether CSV downloads start.
2. Exceptions (`/exceptions`)
   - Attempt to save/apply a template; record any feedback (or lack thereof).
3. Trend Analysis (`/trends`)
   - Run the export action (**Экспортировать в отчёт**); capture the observed behaviour in text (banner? download? nothing?).
4. Absenteeism (`/absenteeism`)
   - Run the calculator for a template and try the export button; log the result (success, error, no-op).
5. Accuracy Dashboard (`/accuracy`)
   - Choose **CSV данные**, click **Создать отчёт**; document whether a file/notification appears.
6. Manual Adjustments (`/adjustments`)
   - Apply a +10% bulk adjustment to a few intervals and save; note any validation/bell feedback.
7. Shell / Notification Bell
   - After each export/action above, open the bell to confirm whether entries were created; record the bell content or note that it stayed empty.

**Real-system comparison**
- Repeat the relevant actions in the Naumen production portal if accessible and note which modules remain non-functional (build banner, exceptions, trends, accuracy, adjustments, bell).

**What to return**
- Text-only Pass/Fail notes for each check (behaviour description, timestamps, console messages if errors appear). Embed them back into this message or in the attached `.txt/.md` log—no images.
- Summarise any console errors (first stack line) and mention if none occurred.

**Where to log findings after the run**
1. Append the Pass/Fail summary to `uat-agent-tasks/forecasting_s04_m01_20251107-1200.md` (this file).
2. Update `docs/Tasks/uat/2025-10-26_forecasting-uat.md` (2025‑11‑07 section) with the new textual notes.
3. Refresh `docs/Tasks/uat/2025-11-07_forecasting-real-vs-demo-validation.md` and `docs/Tasks/uat/2025-11-07_forecasting-illustrated-guide-review.md` if the comparison changed.
4. Log the outcome in `docs/SESSION_HANDOFF.md` and `PROGRESS.md` (deployment + status).
5. If any blockers occur, record them in the same docs and highlight in the prompt reply.

**Ground rules**
- Behaviour parity only; no visual/pixel checks.
- Do **not** fabricate or request screenshots.
- Stop immediately if credentials fail or the environment is unreachable and report the blocker.

---
