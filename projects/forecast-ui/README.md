# MyTKO Forecast UI — Прототип системы прогнозирования ТКО

> Быстрый доступ: см. NEXT_AGENT_HANDOFF.md для инструкций по обзору/запуску.

Начать обзор:
- NEXT_AGENT_HANDOFF.md — краткий гид для следующего агента (что читать и что делать)
- REVIEW_GUIDE.md — как быстро проверить демо (таб→эндпоинт, фолбэки, CSV)
- REVIEW_CHECKLIST.md — чек‑лист смоков и скриншотов
- CHANGES.md — что нового, без ломающих изменений
- HOWTO_E2E.md — как запустить Playwright смоки

Прототип подсистемы прогнозирования для продукта "Чистая логистика" (MyTKO).

## 📋 Обзор проекта

Веб-интерфейс для визуализации прогнозов вывоза твёрдых коммунальных отходов (ТКО) с следующими возможностями:

- **Обзор** — метрики точности прогноза (WAPE) по региону и районам с графиками
- **Районы** — таблица лучших/худших районов по SMAPE с диаграммами
- **Площадки** — прогнозы заполнения контейнерных площадок с фильтрами по риску
- **Маршруты** — рекомендации по маршрутизации (строгая и расширенная политики)

## 🛠 Технологический стек

- **React 18** + TypeScript
- **Vite** — сборщик
- **Tailwind CSS** — стилизация
- **Recharts** — графики и диаграммы
- **Vercel** — deployment платформа

## 📦 Структура проекта

```
forecast-ui/
├── src/
│   ├── components/
│   │   ├── Layout.tsx          # Основной layout с sidebar и tabs
│   │   ├── Sidebar.tsx         # Боковое меню навигации
│   │   ├── Overview.tsx        # Обзор с метриками WAPE
│   │   ├── Districts.tsx       # Таблица районов + pie chart
│   │   ├── Sites.tsx           # Таблица площадок с прогнозами
│   │   └── Routes.tsx          # Рекомендации по маршрутам
│   ├── data/
│   │   └── metrics.ts          # Типы и mock-данные
│   ├── App.tsx                 # Главный компонент
│   ├── main.tsx                # Entry point
│   └── index.css               # Глобальные стили + Tailwind
├── package.json
├── vite.config.ts
├── tailwind.config.js
├── tsconfig.json
└── vercel.json                 # Конфиг для Vercel deployment
```

## 🚀 Инструкции для Claude Code Agent

### Шаг 1: Инициализация проекта

```bash
cd forecast-ui
npm install
```

Это установит все зависимости из `package.json`:
- react, react-dom
- recharts (графики)
- tailwindcss, autoprefixer, postcss
- vite, typescript

### Шаг 2: Запуск в dev режиме

```bash
npm run dev
```

Приложение откроется на `http://localhost:3000`

### Шаг 3: Сборка для production

```bash
npm run build

### Шаг 4: Установка git hooks (рекомендовано)

```
bash scripts/install_hooks.sh
```

Pre-commit hook предупреждает для файлов TS/TSX >350 строк и блокирует коммит >500 строк (если нет временного waiver):

```ts
// @allow-large-file:REF-UI-SPLIT
```
```

Результат сборки будет в директории `dist/`

### Шаг 4: Проверка production build локально

```bash
npm run preview
```

### Шаг 5: Deployment на Vercel

#### Вариант A: Через Vercel CLI

```bash
# Установить Vercel CLI
npm i -g vercel

# Залогиниться
vercel login

# Задеплоить проект
vercel

# Для production deployment
vercel --prod
```

#### Вариант B: Через GitHub + Vercel Dashboard

1. Создать Git репозиторий:
```bash
git init
git add .
git commit -m "Initial commit: MyTKO Forecast UI prototype"
git branch -M main
git remote add origin <your-github-repo-url>
git push -u origin main
```

2. Зайти на [vercel.com](https://vercel.com)
3. Нажать "New Project"
4. Импортировать Git репозиторий
5. Настройки:
   - **Framework Preset**: Vite
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
   - **Install Command**: `npm install`
6. Нажать "Deploy"

Vercel автоматически:
- Установит зависимости
- Соберёт проект
- Задеплоит на CDN
- Даст публичный URL вида `https://your-project.vercel.app`

### Шаг 6: Автоматические деплои

После подключения к GitHub, каждый push в `main` ветку будет автоматически запускать новый deployment на Vercel.

См. также: docs/SOP/TEST_RUN_SOP.md — как запускать e2e тесты с таймаутом и собирать HTML‑отчет/тайминги.

## 🎨 Дизайн-система

Интерфейс реплицирует стиль существующего продукта MyTKO:

- **Sidebar**: Тёмный (`#1f2937`), слева, с иконками
- **Топ-меню**: Светлое, с tabs
- **Акцент**: Зелёный (`#4ade80`) для кнопок
- **Таблицы**: Плотные, sticky headers, hover эффекты
- **Графики**: Минималистичные, цветовое кодирование по уровню риска

## 📊 Данные

Проект использует **mock-данные** из `src/data/metrics.ts`:

- `metricsData` — метрики WAPE/SMAPE из `metrics_summary.json`
- `mockSites` — 8 демо-площадок с прогнозами
- `mockRoutesStrict/Showcase` — рекомендации по маршрутам

**Для продакшена**: заменить mock-данные на API-запросы к бэкенду.

## 🔧 Возможные улучшения

### Короткий срок (1-2 часа)
- [ ] Добавить date picker для выбора даты прогноза
- [ ] Экспорт таблиц в CSV/Excel
- [ ] Dark mode toggle
- [ ] Адаптивность для мобильных (responsive breakpoints)

### Средний срок (1-2 дня)
- [ ] Интеграция с реальным API бэкенда
- [ ] Историческая динамика метрик (line charts)
- [ ] Карта с площадками (Leaflet/Mapbox)
- [ ] Фильтры по районам и перевозчикам
- [ ] WebSocket для real-time обновлений

### Долгий срок (1+ недель)
- [ ] Аутентификация (OAuth/JWT)
- [ ] Права доступа по ролям (диспетчер/мастер/водитель)
- [ ] Редактирование прогнозов вручную
- [ ] Интеграция с существующей системой MyTKO
- [ ] Unit/E2E тесты (Jest, Playwright)

## 🐛 Troubleshooting

### Ошибка: "Cannot find module"
```bash
rm -rf node_modules package-lock.json
npm install
```

### Ошибка при сборке Tailwind
Проверьте, что `postcss.config.js` и `tailwind.config.js` на месте.

### Vercel deployment failed
- Проверьте, что `vercel.json` корректен
- Убедитесь, что build command: `npm run build`
- Проверьте логи на vercel.com/dashboard

## 📝 Заметки

- Проект настроен на Node 18+
- TypeScript strict mode включён
- Все компоненты функциональные (React Hooks)
- CSS-классы через Tailwind utilities
- Нет роутинга (single page), но можно добавить React Router

## 🎯 Acceptance Criteria (из документации)

✅ Пользователи могут открыть один экран и понять качество прогноза  
✅ Есть возможность фильтрации по риску переполнения  
✅ Ссылки на CSV и QA discoverable (в production будет)  
✅ Дизайн соответствует существующему продукту MyTKO  

## 📞 Контакты

Если нужна помощь с развертыванием или есть вопросы — создайте issue в репозитории.

---

**Статус**: ✅ Готов к deployment на Vercel  
**Последнее обновление**: 2025-01-04
