# Vision‑Driven Design (VDD)

This document outlines the vision, domain boundaries, and modular extension points (scenarios, source plugins) for the forecasting system. In PR‑0 we introduce scenario configs and plugin discovery scaffolding without behavior changes. Subsequent PRs will incrementally add features behind flags.

