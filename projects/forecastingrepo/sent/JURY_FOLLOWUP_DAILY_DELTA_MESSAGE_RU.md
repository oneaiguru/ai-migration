Привет!

Посмотрел твою проверку — ты всё правильно посчитал, просто я сам накосячил с форматом и не объяснил.

Файл, который я тебе отправлял (`forecast_jun_dec_2025_jury_format.csv`), оказался **накопительным**: каждая колонка — это сумма с 01.06 (а не «объем за день»). Поэтому если его просто суммировать по всем ячейкам за июнь–октябрь, получается огромная цифра (~248 млн) — это не сопоставимо с вашим отчетом «день‑объём».

Я пересобрал прогноз в правильном виде **«день‑объём» (дельты)**:
- `forecast_jun_dec_2025_jury_format_daily.csv` — значения уже по дням, можно прямо суммировать и сравнивать с фактом.

Формат тот же: `Код КП + объемы по дням`, разделитель `;`, десятичная запятая.

Извини за путаницу — теперь сравнение будет корректным.
