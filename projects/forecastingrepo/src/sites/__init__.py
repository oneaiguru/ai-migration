"""
Sites package: schema and ingest helpers for per-site (контейнерная площадка) data.

This package is added in the Sites S‑series and is unused by the default
forecast pipeline (no behavior change). It is exercised only by PR‑S1 tests.
"""

