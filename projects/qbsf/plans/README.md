# Plans

Store planner output here as `YYYY-MM-DD_<task>.plan.md`.
- Each plan should reference the task brief in `tasks/` and cite file:line anchors.
- Use the CE magic prompts for planner role before writing the plan.
