UI Supporting Bundle

Includes supporting types, utils, and small data files:
- src/types/api.ts — API shapes
- src/utils/demo.ts — demo default date helper
- src/data/metrics.ts — mock metrics (legacy; reference only)

Focus areas:
- Contract expectations for API responses
- Demo date precedence (sessionStorage → env → today)
