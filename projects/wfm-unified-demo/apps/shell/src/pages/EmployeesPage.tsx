import { Root as EmployeesRoot } from 'employee-management'

const EmployeesPage = () => (
  <div className="overflow-hidden rounded-xl border border-shell-divider bg-shell-panel shadow-shell">
    <EmployeesRoot />
  </div>
)

export default EmployeesPage
