# TODO

- Decide whether to keep this project in the monorepo or point to the upstream `tools/whisper_infinity_bot` directly. No reference-mcp copy was found (`.worktrees/reference-mcp` only contains fastwhisper), but re-check before deleting or de-duping.
- Replace the placeholder token in `config.json` with a real value locally before running the bot (do not commit secrets).
